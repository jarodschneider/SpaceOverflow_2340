<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp"
    tools:context=".views.EventActivity">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="20sp"
        android:id="@+id/warping_label"
        android:text="WARPING" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/warping_label"
        android:layout_marginTop="20dp"
        android:id="@+id/event_text"
        android:text="EVENT HERE" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/event_text"
        android:layout_marginTop="12dp"
        android:id="@+id/continue_warp_button"
        android:onClick="onContinuePressed"
        android:text="CONTINUE WARPING" />

</RelativeLayout>